# lint-server-handlers exemptions
#
# 数据来源：2026-07-31 fail 模式切换时按 master 实测重采（前一版基于
# origin/master HEAD 5a7f04aa 2026-05-28）
# 配套：docs/design/server-split-phase4-design.md v0.6.1 §6.2.0.4 + §0 事实速查表
#
# 规则：
# - server 包文件硬上限 limit:500（不是 800）— 见 §9.2 file_size budget
# - dashboard 子包文件硬上限 800（v0.6.1 暂无此条目）
# - 每条必须带 `until: "YYYY-MM-DD"` 绝对到期日（#2561 起）。此前是
#   `until_phase`，而它指向的每个 phase 都已被 ADR-001 暂缓 ——「until Phase 5」
#   实际含义是「永久」，棘轮从不收紧。日期无法被暂缓：过期即 rule 5 卡 PR，
#   强制重新决策而不是静默永久化。缺失或无法解析的日期同样是 violation，
#   所以新条目不能靠省略字段绕过棘轮。带引号避免 YAML 把它解析成 date 类型
# - 文件缩到限制以下时删条目，PR commit 惯例带 "Closes-exemption: <file>" trailer。
#   注意：rule 5 (stale_exemption) 只做 os.Stat 存在性判断，**不校验 commit
#   trailer** —— 全仓没有任何 CI step 做这件事（design.md:757 声称"无对应行
#   即 fail"是文档对代码的虚构，已在 #2551 E1-c 订正）。trailer 是人肉纪律
# - 豁免清单原则上不新增条目；但 baseline `current` 必须随 master 漂移
#   更新（preflight 必检 — 见 §6.2.0.4）
#
# 2026-07-31 fail 模式切换说明：
# - 原计划"Phase 5 完工后切 fail"，但 ADR-001 判定 4b 暂缓 → Phase 5 的
#   触发条件不再会到来，warn 模式事实上被永久化。本次以当日实测行数为
#   baseline 直接切 fail：baseline 冻结当前水位，再长一行即卡 PR。
# - "不新增条目"规则的前提是 phase 会持续落地；4b 暂缓后，R243-ARCH-2
#   从 wshub.go 拆出的 wshub_broadcast.go / wshub_subscribe.go 无豁免
#   会让 fail 模式永久红，故当日为两文件补录条目、与 wshub.go 同挂 4b。
#
# 2026-09-07（#2551 E1-c）重采说明：
# - 那两条补录条目已在本次删除 —— 两个文件后来被别的重构改瘦到 365 / 433
#   行，早已 ≤ 500 上限，条目自 2026-07-31 之后就是死账。send.go 同理
#   （记 599 / 实测 461）。11 → 8 条。
# - 上一条"baseline current 必须随 master 漂移更新"在实践中没被执行：
#   wshub.go 记 1381 实测 742（差 639），dashboard_send.go 记 1089 实测 851。
#   两条已重采。lint 只在 `lines > current` 时报警，所以 baseline 偏高会静默
#   放过增长 —— 重采不是洁癖，是把闸门重新上膛。

file_size:
    # 2026-09-08（#2561 E6-c）：以下四条删除 —— 文件已拆到 800 行上限之下，
    # 按 rule 5(b) 清理。拆分是机械搬迁（一次性脚本 go-split-file.py，逐字节守恒校验；脚本已随 #2637 删除，方法见 PR #2627 描述）：
    #   internal/dashboard/session/handlers.go  1470 → 374（拆 5 文件）
    #   internal/dashboard/project/files.go     1310 → 352（拆 5 文件）
    #   internal/dashboard/cron/handlers.go     1296 → 131（拆 5 文件）
    #   internal/dashboard/cron/transcript.go   1130 → 445（拆 4 文件）
    # internal/dashboard 下已无 >800 行的非测试文件。
    # Closes-exemption: internal/dashboard/session/handlers.go
    # Closes-exemption: internal/dashboard/project/files.go
    # Closes-exemption: internal/dashboard/cron/handlers.go
    # Closes-exemption: internal/dashboard/cron/transcript.go
    # 2026-09-07（#2551 E1-c）重采：11 → 8 个条目。删掉 4 条死账（文件已 ≤ 500
    # 上限，按 rule 5(b) 语义应删；lint 的 scanFileSize 在查豁免表之前就
    # `if lines <= limit { return nil }`，所以它们早已惰性），新增 0 条。
    # 缩水都发生在 #2551 之前的历次重构里，只是从没人重采 baseline。
    # ── > 800 行（dashboard 子包）──
    # internal/server/dashboard_session.go: removed Phase 3e (PR #1441) — file
    # moved to internal/dashboard/session/handlers.go. Closes-exemption trailer
    # in commit message satisfies rule 5.
    # internal/server/project_files.go: removed Phase 2 (PR #1437) — file
    # moved to internal/dashboard/project/files.go. Closes-exemption trailer
    # in commit message satisfies rule 5. New dashboard/project/files.go
    # entry tracks the post-move size (1633) until Phase 5 internal split.
    - path: internal/server/dashboard_send.go
      current: 783                          # 2026-09-09 重采（#2632）：851 → 783，remote-send goroutine / workspace 解析收进 sendEngine 方法。上一次 2026-09-07（#2551 E1-c）1089 → 851
      limit: 500
      until: "2027-03-31"     # 退役者 #2561 E6-c（Epic E 的 handler 文件机械拆分）。历史：3f → "E6" → 日期
    # internal/server/dashboard_cron.go: removed Phase 1 (PR #1438) — file
    # moved to internal/dashboard/cron/handlers.go. Closes-exemption trailer
    # in commit message satisfies rule 5.
    # internal/server/dashboard_cron_transcript.go: removed Phase 1 — file
    # moved to internal/dashboard/cron/transcript.go.
    - path: internal/server/server.go
      current: 702                          # 2026-09-09 −27（#2633：Dispatcher 构造搬到 build_dispatch.go，health 路由搬到 routes.go）；2026-09-07 +3（#2561 E6-a：dashsession Deps 接口化前的 typed-nil 解包，理由注释在代码里）；同日 +18（#2552 E2-a：appCtx/appCancel 字段 + uploadStore 字段 + 组合根前移的三段说明性注释）；2026-09-05 +3（#2538：config fingerprint 三行接线进 HealthHandler）
      limit: 500
      until: "2027-03-31"     # 退役者 #2197（Server god-struct 的余下部分；#2553 E3 已把 handler 字段 18→4）。历史："5" → "E3" → 日期
    - path: internal/server/wshub.go        # v0.6.1 新增豁免：v0.4 时 514 不超线
      current: 748                          # 2026-09-07 +6（#2552 E2-a：HubOptions.UploadStore + 其理由注释，取代 SetUploadStore 后注入）；同日重采（#2551 E1-c）：记 1381 实测 742，差 639 行 —— 期间 R243-ARCH-2 等拆分从未回写 baseline
      limit: 500
      until: "2027-03-31"     # 退役者 Epic K #2549（Hub 拆 Subscriber/Broadcast 子对象）。历史：4b → "K" → 日期
    # internal/server/routes.go: entry removed 2026-09-07（#2552 E2-a）—— 构造段
    # 移到 build_dashboard.go 后降到 472 行，≤ 500 上限；按 rule 5(b) 清理。
    # 它此前一路涨到 553（#2558 的 14 条模块路由 + #2559 的 /static/css/{file}）。
    # 删掉条目意味着再涨过 500 立刻卡 PR，这正是想要的效果。
    # Closes-exemption: internal/server/routes.go
    # internal/server/agent_tailer.go: entry removed 2026-07-31 — tailerRegistry
    # 抽离 (Phase 4c-prep) 后降到 356 行，≤ 500 上限；按 rule 5(b) 语义
    # （文件存在但行数 ≤ limit → 条目应删除）清理豁免债。
    # Closes-exemption: internal/server/agent_tailer.go
    # ── 2026-09-07（#2551 E1-c）删掉的三条死账 ──
    # 三个文件都已 ≤ 500 上限，条目从此惰性；按 agent_tailer.go 的先例（rule
    # 5(b)：文件存在但行数 ≤ limit → 条目应删除）清理。实测行数与条目记录：
    #   internal/server/send.go             461（记 599，挂 3f）
    #   internal/server/wshub_broadcast.go  365（记 653，挂 4b）
    #   internal/server/wshub_subscribe.go  433（记 543，挂 4b）
    # 后两条是 2026-07-31 为 R243-ARCH-2 从 wshub.go 拆出的方法文件补录的，
    # 补录时确实超 500；之后被别的重构改瘦，没人回来删。
    # 至此 until_phase: 4b 只剩 wshub.go 一条，且已改挂 Epic K。
    # Closes-exemption: internal/server/send.go
    # Closes-exemption: internal/server/wshub_broadcast.go
    # Closes-exemption: internal/server/wshub_subscribe.go
    # internal/server/dashboard_auth.go: removed Phase 3a (PR #1436) — file
    # moved to internal/dashboard/auth/handlers.go. Closes-exemption trailer
    # in commit message satisfies rule 5.

# rule 1 (handle_decl) 的唯一豁免。归属规则见 internal/server/doc.go：
# internal/server 只拥有 HTTP 管道（mux/routes、middleware、WS hub、health、
# 静态壳与资源、平台 webhook 注册、send/upload 管线）；其余 /api/* handler
# 一律放 internal/dashboard/<sub> 子包并经 Deps 结构体注入。
handle_baseline:
    - Server.handleDashboard    # 静态壳 GET /dashboard = server 自有
